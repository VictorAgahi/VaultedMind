apiVersion: v1
kind: Service
metadata:
  name: vault-backend
  namespace: vault-prod
spec:
  type: ClusterIP
  selector:
    app: vault-backend
  ports:
  - port: 80
    targetPort: 3000
    name: http
