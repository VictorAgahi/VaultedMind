apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-session
  labels:
    app: ms-session
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ms-session
  template:
    metadata:
      labels:
        app: ms-session
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      initContainers:
        - name: migrate-ms-session
          image: victorcra/masterquizz-ms-session:1.0.0
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "50m"
              memory: "128Mi"
          command: ["/bin/sh", "-c"]
          args:
            - |
              node -e "const net = require('net'); const s = () => { const c = net.createConnection(process.env.POSTGRES_PORT, process.env.POSTGRES_HOST).on('connect', () => process.exit(0)).on('error', () => { console.log('waiting for postgres...'); setTimeout(s, 2000); }); }; s();" && node packages/ms-session/src/run-migrations.js
          envFrom:
            - configMapRef:
                name: global-config
          env:
            - name: YARN_CACHE_FOLDER
              value: "/tmp/yarn-cache"
            - name: POSTGRES_DB
              value: "ms_session_db"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: masterquizz-db-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: masterquizz-db-secrets
                  key: postgres-password
            - name: VALKEY_ENABLED
              value: "false"
            - name: KAFKA_ENABLED
              value: "false"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      containers:
        - name: ms-session
          image: victorcra/masterquizz-ms-session:1.0.0
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          command: ["/bin/sh", "-c"]
          args:
            - "node packages/ms-session/src/index.js"
          livenessProbe:
            httpGet:
              path: /metrics
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /metrics
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: global-config
          env:
            - name: PORT
              value: "3000"
            - name: QUIZZ_SERVICE_URL
              value: "http://ms-quizz-management"
            - name: RESPONSE_SERVICE_URL
              value: "http://ms-response"
            - name: SESSION_SERVICE_URL
              value: "http://ms-session"
            - name: POSTGRES_DB
              value: "ms_session_db"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: masterquizz-db-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: masterquizz-db-secrets
                  key: postgres-password
            - name: VALKEY_ENABLED
              value: "true"
            - name: KAFKA_ENABLED
              value: "true"
            - name: AUTH_INTERNAL_PRIVATE_KEY_PATH
              value: "/app/keys/internal-private.pem"
            - name: AUTH_INTERNAL_PUBLIC_KEY_PATH
              value: "/app/keys/internal-public.pem"
            - name: AUTH_GAME_PRIVATE_KEY_PATH
              value: "/app/keys/game-private.pem"
            - name: AUTH_GAME_PUBLIC_KEY_PATH
              value: "/app/keys/game-public.pem"
            - name: YARN_CACHE_FOLDER
              value: "/tmp/yarn-cache"
            - name: OTEL_SERVICE_NAME
              value: "ms-session"
          volumeMounts:
            - name: auth-keys
              mountPath: "/app/keys"
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: auth-keys
          secret:
            secretName: masterquizz-auth-keys
            items:
              - key: access_private.pem
                path: access-private.pem
              - key: access_public.pem
                path: access-public.pem
              - key: refresh_private.pem
                path: refresh-private.pem
              - key: refresh_public.pem
                path: refresh-public.pem
              - key: internal_private.pem
                path: internal-private.pem
              - key: internal_public.pem
                path: internal-public.pem
              - key: game_private.pem
                path: game-private.pem
              - key: game_public.pem
                path: game-public.pem
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: ms-session
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: ms-session
