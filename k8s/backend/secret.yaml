# This file is for reference only - secrets must be created manually in K8s
# Do NOT commit actual secrets to Git

# Create secrets with:
# kubectl create secret generic vault-backend-secret \
#   --from-literal=jwt-secret='your-jwt-secret-here' \
#   --from-literal=encryption-key='your-encryption-key-here' \
#   -n vault-prod
#
# kubectl create secret generic postgres-secret \
#   --from-literal=username='postgres' \
#   --from-literal=password='your-postgres-password-here' \
#   -n vault-prod
